# Travel Buddy — production Supabase target
# Copy to `.env.local` for local development. Never commit secrets.
# Only the publishable anon key belongs in the Vite client.
# Service-role keys must never be used in the browser.

# Verified project: travel-buddy-production (farnjmgwcayvkzuaoifk)
# Organisation: The Peptides Guy (axqrjaxwqjiqphdhzbcr)
# Do NOT point these at aboss-production, ai-invoicing-app-production, or aleya-logo-creator.

VITE_SUPABASE_URL=https://farnjmgwcayvkzuaoifk.supabase.co
VITE_SUPABASE_ANON_KEY=your_publishable_or_legacy_anon_key
VITE_SUPABASE_PROJECT_REF=farnjmgwcayvkzuaoifk

# Production app origin used for Auth email redirects (set after domain bind).
VITE_APP_URL=https://YOUR_TRAVEL_BUDDY_DOMAIN
